headers {
  Accept: application/json
  Content-Type: application/json
  Origin: {{host}}
  Timezone: Europe/Berlin
}

auth {
  mode: none
}

docs {
  Select the Local environment and run Authentication/Login first. Bruno keeps the HTTP-only session cookie in its cookie jar for subsequent requests.
}
